{% block content %}

{% if tagg %}







{% set mestaches = craft.entries()
    .section('artvers')
    .tagg(tagg.id)
    .limit(99)
    .orderBy('dateCreated DESC')
    .all() %}

{# {% set leurtaches = craft.entries()
    .section('artvers')
    .version(vers.id)
    .gens('not ' ~ user.id)
    .limit(99)
    .orderBy('dateCreated DESC')
    .all() %} #}

{% endif %}

        <span><small>Ressources Utiles :<br><br></small></span>

<div onclick="addoneart({{ tagg.id }})" class="btn btn-outline-danger" >+ Ressource</div>
|
{% for item in mestaches %}
    <a href="/c/{{ item.id }}"><div class="btn btn-outline-secondary" >{{item.article[0].title}}</div></a>
{% endfor %}



{% endblock %}



